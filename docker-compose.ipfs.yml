services:
  ipfs:
    image: ipfs/kubo:v0.28.0
    container_name: vibefi-ipfs
    environment:
      - IPFS_PROFILE=server
    ports:
      - "5001:5001"
      - "8080:8080"
      - "4001:4001"
    volumes:
      - ipfs-data:/data/ipfs

volumes:
  ipfs-data:
